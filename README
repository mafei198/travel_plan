这是一个自己练手的项目功能还在逐步完善中，初学者代码较丑陋多多指点。

travel_plan是干什么的？
  分享游记；
  查看旅游景点；
  制作、分享旅行计划；
  便捷订票、定酒店、租车；
  基于靠谱旅行者的垂直旅行问答；

开发平台：
  ubuntu 10.10 
  ruby 1.8.7
  rails 3.0.7
  mysql

版本管理：
  git
  rvm

ubuntu部署流程：
  sudo apt-get install ruby
  gem install rails
  
  在项目根目录下：
    bundle install
    rake db:create
    rake db:migrate
    在rails console 下输入 doc文件夹下 init_travel_plan_db文件内的内容(初始化测试数据)
    rails s就启动了

默认主页：http://localhost:3000

